# UI Preview

This folder is mock/reference material only.

It illustrates the intended Concept B operator experience with static data. It
is not application source, does not call real APIs, and is not evidence that the
web service, worker, bot, Supabase schema, scheduler, Telegram integration, or
resolver has been implemented.
